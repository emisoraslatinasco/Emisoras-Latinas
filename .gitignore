# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
data_backup_1768689106166/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# agent config
/.agent

# scripts de inspección y descarga (development only)
check-streams.js
inspect-stations.js
download-missing-logos.js
search-missing-logos.js
update-descriptions.js
rewrite-colombia-descriptions.js
rewrite-all-descriptions.js
cleanup-stations.js
normalize-logo-names.js
search-missing-logos.js

# Scripts de enriquecimiento (no se envían a GitHub)
/scripts/
/** @type {import('next').NextConfig} */
const nextConfig = {
  // ... otras configuraciones que ya tengas (como lo de imágenes) ...
  
  typescript: {
    // ⚠️ ATENCIÓN: Ignora errores de tipado para permitir el build
    ignoreBuildErrors: true,
  },
  eslint: {
    // ⚠️ ATENCIÓN: Ignora errores de linter para permitir el build
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;